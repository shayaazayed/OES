# دليل رفع المشروع إلى Railway (الخيار الأفضل والشامل)

يُعتبر **[Railway](https://railway.app/)** خياراً ممتازاً لأنه يتيح لك استضافة **كل شيء في مكان واحد** (قاعدة البيانات + السيرفر + الواجهة) وبسهولة شديدة.

## 1. إنشاء المشروع وقاعدة البيانات
1.  سجل الدخول في [Railway](https://railway.app/).
2.  انقر على **New Project** > **Provision MySQL**.
3.  سيتم إنشاء قاعدة بيانات MySQL لك فوراً.
4.  انقر على بطاقة **MySQL** التي ظهرت، ثم اذهب إلى تبويب **Variables**.
5.  انسخ قيمة `MYSQL_URL` (أو `DATABASE_URL`). ستحتاجها لربط السيرفر.

**ملاحظة هامة:** Railway يوفر لك أداة لرفع ملف `Database.sql` الخاص بك عبر المتصفح (Query Browser) أو يمكنك الاتصال بالقاعدة خارجياً باستخدام برنامج مثل MySQL Workbench ورفع الجداول.

---

## 2. رفع الـ Backend (السيرفر)
1.  في نفس المشروع، اضغط **New** > **GitHub Repo**.
2.  اختر مستودع مشروعك `ExamSystem`.
3.  بعد إضافته، اضغط على بطاقة الخدمة الجديدة (التي تحمل اسم مشروعك) > **Settings**.
4.  غيّر **Root Directory** إلى `/Backend`.
5.  اذهب إلى تبويب **Variables** وأضف:
    *   `ConnectionStrings__DefaultConnection`: ألصق رابط قاعدة البيانات (`MYSQL_URL`) الذي نسخته في الخطوة الأولى.
    *   `Jwt__Key`: `YourSecretKeyHereThatIsAtLeast32CharactersLong!`
    *   `Jwt__Issuer`: `ExamSystem`
    *   `Jwt__Audience`: `ExamSystemUsers`
    *   `PORT`: `5000` (مهم جداً ليعرف Railway المنفذ الصحيح).
6.  سيقوم Railway بإعادة بناء المشروع وتشغيله تلقائياً.

---

## 3. رفع الـ Frontend (الواجهة)
1.  اضغط **New** > **GitHub Repo** مرة أخرى (نفس المستودع).
2.  اضغط على البطاقة الجديدة > **Settings**.
3.  غيّر **Root Directory** إلى `/Frontend`.
4.  بما أننا أضفنا `Dockerfile` للمجلد، سيكتشفه Railway ويقوم ببناء حاوية Nginx لخدمة ملفاتك.
5.  اذهب إلى تبويب **Settings** > **Domains** واضغط **Generate Domain** لتحصل على رابط عام للموقع (مثلاً: `frontend-production.up.railway.app`).

---

## 4. الخطوة الأخيرة: الربط
1.  انسخ رابط الـ Backend من تبويب **Settings/Domains** الخاص بخدمة الـ Backend.
2.  عدل ملف `Frontend/js/api.js` في جهازك:
    ```javascript
    // استبدل الرابط القديم برابط Railway الجديد
    this.baseURL = 'https://backend-production.up.railway.app/api'; 
    ```
3.  ارفع التعديل إلى GitHub:
    ```bash
    git add .
    git commit -m "Update API URL for Railway"
    git push
    ```
4.  سيقوم Railway بتحديث الـ Frontend تلقائياً خلال دقائق.

وهكذا يكون لديك نظام كامل (قاعدة بيانات + سيرفر + واجهة) يعمل على منصة واحدة! 🚀
