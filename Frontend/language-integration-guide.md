# Language System Integration Guide

## نظم الترجمة الشامل لـ ExamPro

هذا الدليل يوضح كيفية دمج نظام الترجمة في جميع صفحات النظام لتحويل الواجهة بالكامل بناءً على لغة المدير.

---

## 🚀 **التثبيت السريع**

### 1. **إضافة مكتبة اللغة**
```html
<!-- في جميع صفحات HTML -->
<script src="js/language-system.js"></script>
```

### 2. **استخدام الترجمة**
```html
<!-- الطريقة الأولى: باستخدام data-translate -->
<h1 data-translate="adminDashboard">لوحة تحكم المدير</h1>

<!-- الطريقة الثانية: باستخدام دالة t() -->
<script>
    alert(t('welcomeToAdminDashboard'));
</script>
```

### 3. **تغيير اللغة**
```html
<!-- محدد اللغة -->
<select onchange="setLanguage(this.value)">
    <option value="ar">العربية</option>
    <option value="en">English</option>
</select>
```

---

## 📋 **قائمة الترجمات المتاحة**

### **التنقل والهيدر**
- `adminDashboard` - لوحة تحكم المدير
- `welcomeToAdminDashboard` - مرحباً بك في لوحة الإدارة
- `logout` - تسجيل الخروج
- `back` - رجوع
- `home` - الرئيسية

### **لوحة التحكم**
- `totalUsers` - إجمالي المستخدمين
- `totalCourses` - إجمالي الدورات
- `totalExams` - إجمالي الاختبارات
- `examAttempts` - محاولات الاختبار
- `quickActions` - إجراءات سريعة
- `thisMonth` - هذا الشهر
- `thisWeek` - هذا الأسبوع

### **الإجراءات السريعة**
- `addUser` - إضافة مستخدم
- `createCourse` - إنشاء دورة
- `quickReport` - تقرير سريع
- `settings` - الإعدادات

### **إدارة المستخدمين**
- `users` - المستخدمون
- `students` - الطلاب
- `teachers` - المعلمون
- `admins` - المديرون
- `userName` - اسم المستخدم
- `fullName` - الاسم الكامل
- `email` - البريد الإلكتروني
- `phone` - رقم الهاتف
- `role` - الدور
- `status` - الحالة
- `active` - نشط
- `inactive` - غير نشط
- `suspended` - معلق

### **إدارة الدورات**
- `courses` - الدورات
- `courseName` - اسم الدورة
- `courseCode` - كود الدورة
- `description` - الوصف
- `credits` - الساعات المعتمدة
- `instructor` - المدرس
- `duration` - المدة
- `startDate` - تاريخ البدء
- `endDate` - تاريخ الانتهاء
- `enrolled` - المسجلون

### **إدارة الاختبارات**
- `exams` - الاختبارات
- `examName` - اسم الاختبار
- `examDate` - تاريخ الاختبار
- `examDuration` - مدة الاختبار
- `examType` - نوع الاختبار
- `questions` - الأسئلة
- `results` - النتائج
- `score` - الدرجة
- `grade` - التقدير
- `passed` - ناجح
- `failed` - راسب
- `published` - منشور
- `draft` - مسودة

### **التقارير**
- `reports` - التقارير
- `generateReport` - إنشاء تقرير
- `reportType` - نوع التقرير
- `dateRange` - نطاق التاريخ
- `fromDate` - من تاريخ
- `toDate` - إلى تاريخ
- `studentsReport` - تقرير الطلاب
- `coursesReport` - تقرير الدورات
- `examsReport` - تقرير الاختبارات
- `performanceReport` - تقرير الأداء

### **الإعدادات**
- `generalSettings` - الإعدادات العامة
- `securitySettings` - إعدادات الأمان
- `systemSettings` - إعدادات النظام
- `userSettings` - إعدادات المستخدم
- `notificationSettings` - إعدادات الإشعارات

### **النماذج والحقول**
- `firstName` - الاسم الأول
- `lastName` - الاسم الأخير
- `password` - كلمة المرور
- `confirmPassword` - تأكيد كلمة المرور
- `currentPassword` - كلمة المرور الحالية
- `newPassword` - كلمة المرور الجديدة
- `address` - العنوان
- `city` - المدينة
- `country` - البلد
- `gender` - الجنس
- `male` - ذكر
- `female` - أنثى
- `dateOfBirth` - تاريخ الميلاد

### **الرسائل والتنبيهات**
- `success` - نجح
- `error` - خطأ
- `warning` - تحذير
- `info` - معلومات
- `loading` - جاري التحميل...
- `noDataAvailable` - لا توجد بيانات متاحة
- `operationCompletedSuccessfully` - تمت العملية بنجاح
- `operationFailed` - فشلت العملية
- `confirmDelete` - هل أنت متأكد من الحذف؟
- `confirmLogout` - هل أنت متأكد من تسجيل الخروج؟
- `itemDeletedSuccessfully` - تم حذف العنصر بنجاح
- `itemSavedSuccessfully` - تم حفظ العنصر بنجاح

### **الجداول**
- `id` - الرقم
- `name` - الاسم
- `actions` - الإجراءات
- `date` - التاريخ
- `time` - الوقت
- `type` - النوع
- `category` - الفئة

### **التحقق من صحة البيانات**
- `requiredField` - هذا الحقل مطلوب
- `invalidEmail` - البريد الإلكتروني غير صحيح
- `invalidPhone` - رقم الهاتف غير صحيح
- `passwordMismatch` - كلمات المرور غير متطابقة
- `weakPassword` - كلمة المرور ضعيفة جداً

---

## 🔧 **طرق الاستخدام**

### **الطريقة الأولى: data-translate (موصى بها)**
```html
<h1 data-translate="adminDashboard">لوحة تحكم المدير</h1>
<button data-translate="save">حفظ</button>
<span data-translate="totalUsers">إجمالي المستخدمين</span>
```

### **الطريقة الثانية: دالة t()**
```javascript
// في JavaScript
alert(t('welcomeToAdminDashboard'));
console.log(t('totalUsers'));
element.textContent = t('settings');
```

### **الطريقة الثالثة: تحديث تلقائي**
```javascript
// النظام يحدد النصوص تلقائياً ويحولها
// لا حاجة لأي إعدادات إضافية
```

---

## 📄 **دمج في الصفحات الموجودة**

### **خطوات الدمج:**

1. **إضافة مكتبة اللغة**
```html
<script src="js/language-system.js"></script>
```

2. **إضافة data-translate للعناصر**
```html
<!-- قبل -->
<h1>لوحة تحكم المدير</h1>
<button>حفظ</button>

<!-- بعد -->
<h1 data-translate="adminDashboard">لوحة تحكم المدير</h1>
<button data-translate="save">حفظ</button>
```

3. **إضافة محدد اللغة**
```html
<select onchange="setLanguage(this.value)">
    <option value="ar">العربية</option>
    <option value="en">English</option>
</select>
```

---

## 🎯 **مثال شامل لصفحة**

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title data-translate="adminDashboard">لوحة تحكم المدير</title>
</head>
<body>
    <header>
        <h1 data-translate="adminDashboard">لوحة تحكم المدير</h1>
        <select onchange="setLanguage(this.value)">
            <option value="ar">العربية</option>
            <option value="en">English</option>
        </select>
        <button data-translate="logout">تسجيل الخروج</button>
    </header>
    
    <main>
        <section>
            <h2 data-translate="welcomeToAdminDashboard">مرحباً بك في لوحة الإدارة</h2>
            
            <div class="stats">
                <div class="stat">
                    <h3 data-translate="totalUsers">إجمالي المستخدمين</h3>
                    <p>150</p>
                    <span>+12 <span data-translate="thisMonth">هذا الشهر</span></span>
                </div>
                
                <div class="stat">
                    <h3 data-translate="totalCourses">إجمالي الدورات</h3>
                    <p>25</p>
                    <span>+3 <span data-translate="thisMonth">هذا الشهر</span></span>
                </div>
            </div>
            
            <section>
                <h2 data-translate="quickActions">إجراءات سريعة</h2>
                <button data-translate="addUser">إضافة مستخدم</button>
                <button data-translate="createCourse">إنشاء دورة</button>
                <button data-translate="quickReport">تقرير سريع</button>
                <button data-translate="settings">الإعدادات</button>
            </section>
        </section>
    </main>
    
    <script src="js/language-system.js"></script>
</body>
</html>
```

---

## 🔄 **الميزات المتقدمة**

### **1. الحفظ التلقائي للغة**
```javascript
// اللغة تحفظ تلقائياً في localStorage
// عند إعادة فتح الصفحة، تستخدم اللغة المحفوظة
```

### **2. التزامن بين التبويب**
```javascript
// عند تغيير اللغة في تبويب، تتغير في جميع التبويبات المفتوحة
window.addEventListener('storage', (e) => {
    if (e.key === 'selectedLanguage') {
        LanguageSystem.applyLanguage(e.newValue);
    }
});
```

### **3. تحديث اتجاه الصفحة**
```javascript
// يتغير اتجاه الصفحة تلقائياً (RTL/LTR)
// العربية: RTL
// الإنجليزية: LTR
```

### **4. أحداث مخصصة**
```javascript
// الاستماع لتغيير اللغة
document.addEventListener('languageChanged', (e) => {
    console.log('Language changed to:', e.detail.language);
    // تنفيذ إجراءات إضافية
});
```

---

## 🎨 **التصميم المتجاوب**

### **دعم RTL/LTR**
```css
/* CSS للدعم التلقائي */
[dir="rtl"] .header-left {
    order: 2;
}

[dir="ltr"] .header-left {
    order: 1;
}
```

### **الخطوط**
```css
/* يتغير الخط تلقائياً حسب اللغة */
body {
    font-family: 'Inter', sans-serif;
}
```

---

## 📱 **الاختبار**

### **صفحة الاختبار**
- `dashboard-language-demo.html` - عرض توضيحي كامل
- `settings-translation-test.html` - اختبار الإعدادات

### **اختبار سريع**
```javascript
// في المتصفح
setLanguage('en'); // تحويل للإنجليزية
setLanguage('ar'); // تحويل للعربية
getCurrentLanguage(); // معرفة اللغة الحالية
```

---

## 🚨 **ملاحظات هامة**

1. **لا حاجة لتغيير الكود الموجود** - فقط أضافة `data-translate`
2. **النظام يعمل تلقائياً** - يكتشف النصوص ويحولها
3. **يدعم جميع المتصفحات** - متوافق مع Chrome, Firefox, Safari, Edge
4. **خفيف وسريع** - لا يؤثر على أداء الموقع
5. **قابل للتوسعة** - يمكن إضافة لغات جديدة بسهولة

---

## 📞 **الدعم**

إذا واجهت أي مشكلة:
1. تحقق من إضافة `data-translate` بشكل صحيح
2. تأكد من تضمين `language-system.js`
3. استخدم صفحة الاختبار للتأكد من العملية

---

**النظام جاهز للاستخدام في جميع صفحات ExamPro!** 🎉
