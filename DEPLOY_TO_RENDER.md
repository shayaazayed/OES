# دليل رفع المشروع إلى Render (Exam System)

هذا الدليل يشرح كيفية رفع المشروع من GitHub إلى منصة [Render](https://render.com).

## 1. تجهيز قاعدة البيانات MySQL
بما أن مشروعك يعتمد على MySQL، ستحتاج إلى قاعدة بيانات تعمل على الإنترنت (Render لا يدعم MySQL مباشرة).

1.  أنشئ حساباً مجانياً على موقع مثل **[Clever Cloud](https://www.clever-cloud.com/)** أو **[Railway](https://railway.app/)** الذي يوفر MySQL.
2.  احصل على **رابط الاتصال (Connection String)** الخاص بقاعدة البيانات.
    -   سيكون الرابط بهذا الشكل: `mysql://username:password@host:port/database_name`
3.  شغل السكربت `Backend/Scripts/Database.sql` على قاعدة البيانات الجديدة للتأكد من وجود الجداول.

---

## 2. رفع الـ Backend (الخادم)
1.  سجل الدخول إلى **[Render Dashboard](https://dashboard.render.com/)**.
2.  انقر على `New` واختر `Web Service`.
3.  اختر مستودع GitHub الخاص بك.
4.  املأ الإعدادات كالتالي:
    *   **Name:** `examsystem-api` (أو أي اسم تفضله)
    *   **Root Directory:** `Backend`
    *   **Runtime:** `Docker` (سيكتشف Dockerfile تلقائياً)
    *   **Region:** `Frankfurt` (الأقرب للشرق الأوسط)
    *   **Plan:** `Free`
5.  في قسم **Advanced**، أضف المتغيرات التالية (Environment Variables):
    *   `ConnectionStrings__DefaultConnection`: ألصق هنا رابط الاتصال بقاعدة البياناتMySQL الذي حصلت عليه في الخطوة الأولى.
       *(تأكد من تعديله ليوافق صيغة Connection String الخاصة بـ .NET إذا لزم الأمر، مثل: `Server=host;Database=db;Uid=use;Pwd=pass;`)*
    *   `Jwt__Key`: `YourSecretKeyHereThatIsAtLeast32CharactersLong!` (نفس المفتاح الموجود في appsettings.json أو غيّره لشيء أقوى).
    *   `Jwt__Issuer`: `ExamSystem`
    *   `Jwt__Audience`: `ExamSystemUsers`
6.  اضغط `Create Web Service`.
7.  انتظر حتى يكتمل النشر (Deployment). بعد الانتهاء، ستجد رابطاً للخدمة (مثلاً: `https://examsystem-api.onrender.com`).
    *   احفظ هذا الرابط لأنك ستحتاجه في الخطوة التالية.

---

## 3. رفع الـ Frontend (الواجهة)
1.  عد إلى **[Render Dashboard](https://dashboard.render.com/)**.
2.  انقر على `New` واختر `Static Site`.
3.  اختر نفس مستودع GitHub.
4.  املأ الإعدادات كالتالي:
    *   **Name:** `examsystem-web`
    *   **Root Directory:** `Frontend`
    *   **Build Command:** (اتركه فارغاً)
    *   **Publish Directory:** `.` (نقطة، أو اتركه فارغاً إذا كان الافتراضي هو public)
5.  اضغط `Create Static Site`.
6.  انتظر النشر. سيظهر لك رابط الموقع (مثلاً: `https://examsystem-web.onrender.com`).

---

## 4. ربط الواجهة بالخادم (تحديث الرابط)
الآن بعد أن أصبح لديك رابط للـ Backend ورابط للـ Frontend:

1.  افتح ملف `Frontend/js/api.js` في مشروعك على جهازك.
2.  ابحث عن السطر:
    ```javascript
    this.baseURL = 'http://localhost:5000/api';
    ```
3.  استبدله برابط الـ Backend الجديد الذي حصلت عليه من Render (مع إضافة `/api` في النهاية)، مثال:
    ```javascript
    this.baseURL = 'https://examsystem-api.onrender.com/api';
    ```
4.  احفظ الملف.
5.  ارفع التعديلات إلى GitHub:
    ```bash
    git add .
    git commit -m "Update API URL for production"
    git push
    ```
6.  ستلاحظ في Render أن الـ Frontend سيعيد بناء نفسه تلقائياً بالتحديث الجديد.

والآن أصبح موقعك جاهزاً ويعمل بالكامل على الإنترنت! 🚀
